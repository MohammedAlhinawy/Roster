# Copy to .env.local and fill in. The publishable key is safe to ship to the
# browser; the secret/service-role key must NEVER go into client code or be
# committed (use it only in server-side code / scripts if needed).
NEXT_PUBLIC_SUPABASE_URL=https://eyjdbbgmdklgwkuozkpz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_publishable_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_secret_key_here   # server-side only