# Netlify + Next.js (server build). Requires the official Next.js plugin —
# the app needs SSR (auth middleware + cookies), so it is no longer a
# static export site.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"